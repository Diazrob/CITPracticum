@using CITPracticum.Data.Enum;
@using CITPracticum.ViewModels;

@model EditEmployerViewModel

@{
    ViewData["Title"] = "Edit an Employer";
}

<div class="row">
    <div class="col-md-6 offset-3">
        <p>
            <h4>Edit an Employer</h4>
        </p>
        @if (TempData["Error"] != null)
        {
            <div class="col-md-12 alert alert-danger">
                <span><b>Sorry! </b>@TempData["Error"]</span>
            </div>
        }
        <div class="row">
            <div class="col-md-8">

                <form method="post" asp-action="Edit" enctype="multipart/form-data">
                    <input type="hidden" value="Id" id="Id" />
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <div class="form-group">
                        <label asp-for="FirstName" class="control-label">First Name:</label>
                        <input asp-for="FirstName" class="form-control" />
                        <span asp-validation-for="FirstName" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label asp-for="LastName" class="control-label mt-2">Last Name:</label>
                        <input asp-for="LastName" class="form-control" />
                        <span asp-validation-for="LastName" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label asp-for="CompanyName" class="control-label mt-2">Company Name:</label>
                        <input asp-for="CompanyName" class="form-control" />
                        <span asp-validation-for="CompanyName" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label asp-for="EmpEmail" class="control-label mt-2">Employer Email:</label>
                        <input asp-for="EmpEmail" class="form-control" />
                        <span asp-validation-for="EmpEmail" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label for="affiliationAgreement" class="control-label mt-2">Does the employer currently have an affiliation agreement?</label>
                        <select id="affiliationAgreement" name="affiliationAgreement" class="form-control">
                            <option value="">Select...</option>
                            <option value="Yes">Yes</option>
                            <option value="No">No</option>
                        </select>
                    </div>
                   
                    <div class="form-group mt-2">
                        <input class="btn btn-outline-success" type="submit" value="Edit Employer" />
                        <a class="btn btn-outline-secondary" asp-controller="Employer" asp-action="Index">Cancel</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>



